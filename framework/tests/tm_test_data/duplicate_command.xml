<!-- Test that commands with duplicate names are not allowed. -->

<commands>
    <!-- The first command is completely legal -->
    <command name="TEST1">
        <component indicator="SPEECHACT"/>
    </command>

    <!-- The second command cannot have the same name -->
    <command name="TEST1">
        <component indicator="SPEECHACT"/>
    </command>
</commands>